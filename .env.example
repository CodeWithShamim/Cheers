# Unicity testnet2 gateway API key.
#
# This key is PUBLIC and NOT a secret: it is the published testnet2 key from the
# Sphere SDK's own .env.example / README (see "Testnet2 endpoints" section of
# node_modules/@unicitylabs/sphere-sdk/README.md). It is safe to keep in client
# code and docs. A MAINNET key, by contrast, would be a real secret — never
# commit one.
VITE_UNICITY_API_KEY=sk_ddc3cfcc001e4a28ac3fad7407f99590
